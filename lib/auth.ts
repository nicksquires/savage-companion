// import NextAuth from "next-auth";
// import { authOptions } from "@/app/api/auth/[...nextauth]/authOptions";

// export const { 
//   handlers, // Used in route.ts
//   auth,     // Used to get the session in Server Components/Actions
//   signIn,   // Used in Server Actions to trigger login
//   signOut   // Used in Server Actions to trigger logout
// } = NextAuth(authOptions);